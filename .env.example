# Image Chat 环境变量配置示例
#
# 使用说明：
# 1. 复制此文件为 .env
# 2. 根据需要修改配置值
# 3. 重启应用使配置生效

# ===========================================
# 默认服务商配置
# ===========================================
# 支持的服务商: google, openrouter, tuzi
# 默认值: google
DEFAULT_PROVIDER=google

# ===========================================
# 默认温度设置
# ===========================================
# 编辑模式默认温度（范围 0.0-1.0）
# 默认值: 0.7
DEFAULT_TEMPERATURE_EDIT=0.7

# 生成模式默认温度（范围 0.0-1.0）
# 默认值: 0.8
DEFAULT_TEMPERATURE_GENERATE=0.8

# ===========================================
# 兔子 API 配置
# ===========================================
# 兔子 API Base URL（可选，用于自托管或测试环境）
# 默认值: https://api.tu-zi.com/v1
TUZI_BASE_URL=https://api.tu-zi.com/v1

# ===========================================
# OpenRouter Headers 配置（可选）
# ===========================================
# HTTP Referer 头（OpenRouter 要求）
# 默认值: http://localhost:5000
# 生产环境建议设置为实际域名
OPENROUTER_REFERER=http://localhost:5000

# X-Title 头（应用标识，OpenRouter 要求）
# 默认值: Image CHAT
OPENROUTER_TITLE=Image CHAT

# ===========================================
# 注意事项
# ===========================================
# - 环境变量优先级高于代码中的默认值
# - 修改后需要重启应用才能生效
# - API Key 不应存储在环境变量中，应通过前端 UI 配置
